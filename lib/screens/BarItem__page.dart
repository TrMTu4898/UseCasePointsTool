import 'package:flutter/widgets.dart';

class BarItem__page extends StatelessWidget {
  const BarItem__page({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
      child: Center(
        child: Text('my__page'),
      ),
    );
  }
}